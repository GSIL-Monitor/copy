1、与数据库或者与外网的交互，不论是什么形式，都可以考虑放入DAO层进行统一管理
2、事务层主要结合产品设计放入逻辑执行顺序。保证原子性和一致性。
3、控制层主要负责为事务层获取参数，处理参数，并对返回的响应进行处理。
4、整个流程中主要在控制层进行try catch，其他层尽量不要把异常吃掉，因为事务层框架可能需要事务及其以下的层抛出事务进行回滚。
5、方法用MAP做参数和用具体多个参数做入参的区别：MAP中修改参数可以实现在方法定义和调用时不做修改，而具体定义多少方法参数
在修改时必须修改方法入参，并在调用的地方一并修改。但这样也有利于查看方法的具体使用情况。